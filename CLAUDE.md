# 종달새 서재 — 작업 안내

개인 아카이브 사이트. GitHub Pages 정적 사이트이고 런타임 빌드가 없다.
주소는 `https://jongdalsae11.github.io`, 로컬 폴더가 곧 사이트다.

구조·기능 설명은 `README.md` 에, 글 쓰는 법은
`posts/2026-08-17-writing-guide.html` 에 있다. 이 파일은 **손대기 전에
알아야 할 것들**만 적는다.

---

## 지켜야 할 것

- **대화도 코드 주석도 한국어.** 주석은 "무엇을 하는가"가 아니라
  **"왜 이렇게 했는가"**를 적는다. 특히 한 번 밟은 지뢰는 반드시 주석으로 남긴다.
  기존 파일들의 주석 톤을 그대로 따라갈 것.
- **`git push` 는 사용자가 직접 한다.** 커밋 명령을 만들어 주는 것까지가 내 몫이다.
- **`assets/data/content.js` 가 유일한 데이터 원본.** 글·자료·시뮬레이션·문제·연구
  목록이 전부 여기 있고, 트리·목록·홈·그래프·검색이 여기서 자동으로 만들어진다.
- **자동 생성 파일을 손으로 고치지 않는다** — `sitemap.xml`, `feed.xml`,
  각 HTML 의 `?v=` 캐시 번호, 글 HTML 의 메타태그·`data-cat`·`data-crumb`.
  전부 `tools/build.mjs` 가 만든다.
- 라이브러리를 새로 들이지 않는다. 바닐라 JS(ES5 문법), CSS 변수, 빌드 도구 없음.
  외부에서 받아 오는 것은 KaTeX·highlight.js·폰트뿐이다.

---

## 손대기 전에 알아야 할 것

여기 적힌 것들은 전부 **실제로 한 번 깨졌던** 것들이다.

### 글쓰기 도구 (`write.js` · `editor.js` · `compose.js` · `save.js`)

- **편집칸에 글자를 넣을 때는 `document.execCommand('insertText')`**
  (`WRITE.applyEdit`). `ed.value = …` 나 `setRangeText` 를 쓰면 브라우저의
  실행취소 스택이 통째로 날아가 `Ctrl+Z` 가 죽는다.
- **`inline()` 은 수식을 먼저 가려 두고 마지막에 되돌린다.** LaTeX 은
  `^[`, `*`, `[[`, `{{`, `` ` `` 를 일상적으로 쓰기 때문이다. 인라인 규칙을
  새로 넣는다면 반드시 마스킹 **뒤에** 넣는다.
- **파서(`parse`)에 블록 규칙을 새로 넣을 때는 두 곳을 같이 고친다** —
  규칙 자체와, 문단 누적 루프의 종료 조건. 그리고 어떤 규칙에도 걸리지 않는
  줄은 반드시 소비해야 한다(안전장치가 있지만 새 규칙이 그걸 우회할 수 있다).
  `i` 가 멈추면 탭이 얼어붙는다.
- **파서 출력의 `data-src` 는 원고 줄번호**다. `sync.js` 의 양방향 이동
  (Ctrl+클릭)이 여기 의존한다. 내보낼 때만 지운다(`bodyHTMLClean`).
- **미리보기는 한 글자마다 통째로 다시 그려진다.** 그래서 상태를 가진 것
  (시뮬레이션 액자)은 `reuse` 로 옮겨 온다. 새로 뭘 붙인다면 이걸 고려할 것.
- **저장은 `content.js` 의 배열만 갈아 끼운다**(`fs.js` 의 `matchBracket` /
  `replaceArray`). 파일명이나 날짜를 바꾸면 이름 바꾸기로 처리되어
  `retarget()` 이 다른 글의 `links` 와 `research.post` 까지 고쳐 준다.
  저장 뒤에는 반드시 `markEditing` 을 다시 걸어야 한다 — 안 그러면 다음 저장이
  새 글로 취급되어 중복 파일이 생긴다.
- **«수정» 으로 글을 열 때는 파일명 칸(`f-slug`)도 같이 채워야 한다**
  (`compose.js` 의 `loadPost`). 비워 두면 `fileBase()` 가 제목에서 자동으로
  만드는데, 한글 제목은 로마자로 떨어진다. 그래서 태그 하나만 고쳐 저장해도
  이름 바꾸기가 발동해서, 확인하면 주소가 깨지고 취소하면 같은 글이 두 벌
  남는다. 실제로 당해서 파일을 손으로 되돌린 적이 있다.

### 글 HTML 파일

- 글 파일 안에는 **원고가 `<script type="text/markdown" class="post-src">` 로
  같이 들어 있다.** 본문 HTML 만 고치면 «수정» 기능이 옛 원고를 되살려
  방금 고친 것을 덮어쓴다. **본문과 원고를 항상 함께** 고칠 것.
  (원고를 고치고 같은 파서로 본문을 다시 그리는 쪽이 안전하다)

### 수식

- KaTeX 는 **글 본문에서만** 돈다. 목록·카드·백링크·이전다음·그래프·검색·
  `<title>`·OG·RSS 처럼 KaTeX 가 닿지 않는 곳은 `U.mathify`(HTML) 와
  `U.mathPlain`(순수 텍스트)으로 유니코드 근사치를 만들어 쓴다.
- 글자 수를 셀 때(읽는 시간·목차)는 `.katex` 를 먼저 걷어낸다. KaTeX 는 같은
  수식을 MathML·TeX 주석·HTML 로 **세 벌** 넣어 두기 때문에 그냥 세면 부풀려진다.
- `write.html` 의 auto-render 스크립트에는 `onload` 훅이 있어야 한다.
  없으면 KaTeX 가 늦게 도착했을 때 미리보기가 영영 안 그려진다.

### 시뮬레이션 (`sims/` · `sim.js` · `U.simFrames`)

- 시뮬레이션 하나 = `sims/` 안의 페이지 하나. 글에서는 `{{Sim-XX}}` 로 부른다
  (자료 인용 `{{Ref-XX}}` 와 같은 이름 공간).
- 글에 끼울 때는 **iframe** 이다. 부모 쪽이 `U.simFrames`, 자식 쪽이 `sim.js`.
  높이는 자식이 `postMessage` 로 알려 준다 — 보낸 창(`e.source`)으로 먼저 찾고,
  안 되면 `ref` 로 찾는다.
- `?embed=1` 이면 `window.EMBED` 가 서고 `nav.js` 가 아무것도 그리지 않는다.
  시뮬레이션 페이지를 새로 만들면 `<head>` 의 그 한 줄을 빼먹지 말 것.
- 효율 비교가 아니라 **원리**를 보여 주는 쪽으로 만든다. 걸음마다 무슨 일을
  왜 했는지 `.sm-say` 에 한 문장씩 적는 것이 핵심이다.

### 그 밖

- 배포는 GitHub Actions 가 **Pages 아티팩트로** 올린다. 되커밋하지 않으므로
  `git pull` 없이 `git push` 만 하면 된다. (예전에 되커밋하다가 push 가
  계속 거부됐던 적이 있다 — 되돌리지 말 것)
- `localStorage` 키: `write-drafts`(초안 목록 — 제목·시각만) ·
  `write-draft:<id>`(초안 하나의 내용) · `write-draft-cur`(지금 열린 초안) ·
  `tree-open` · `graph-moved`.
  초안을 한 칸에 몰아넣지 않은 것은 일부러다 — `save()` 가 타자 한 번마다
  도는데, 묶어 두면 매번 초안 전부를 다시 직렬화해야 해서 타자가 밀린다.
  (`write-draft` 는 옛 단일 초안 칸. 첫 로드 때 한 번 옮겨 오고 지운다)
- **초안을 갈아탈 때는 «고치는 중»(`markEditing`) 도 함께 옮겨야 한다.**
  띠가 남으면 새로 쓴 글이 엉뚱한 글을 덮어쓴다. 초안에 `editing` 이 없으면
  `markEditing(null)` 을 반드시 부를 것.
- CSS 를 고친 뒤에는 중괄호 짝이 맞는지 본다. 여분의 `}` 하나가 조용히
  뒤쪽 규칙을 통째로 죽인 적이 있다.
- 검색과 슬래시 메뉴는 `U.matches` 를 쓴다 — 두벌식 자판 상태로 친 한글
  (`/wmdaud` → 증명)까지 걸린다.

---

## 고치고 나서 돌릴 것

```bash
node tools/build.mjs        # 데이터·링크 검사 + 생성 파일·캐시번호 갱신
npm i jsdom                 # (한 번만)
node tools/smoke.mjs        # 모든 페이지를 실제로 열어 스크립트 오류 확인
```

`build.mjs` 는 여러 번 돌려도 같은 결과가 나와야 한다. 두 번째 실행에서
"생성 파일 최신 상태" 가 나오면 정상이다.

CSS·JS 를 고쳤으면 `build.mjs` 를 돌려 `?v=` 를 새로 찍어야 브라우저가
옛 파일을 계속 쓰지 않는다.

---

## 열린 항목

- 저장소 **Settings → Pages → Source** 가 `GitHub Actions` 여야 배포가 실제로
  올라간다. (확인된 적 없음 — 사이트가 갱신 안 되면 여기부터 볼 것)
